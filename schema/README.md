# Schema

This directory hosts schema material for Atomic Rule Cards.

`rule-card.schema.json` validates the structure of a rule card: identity, layer, applicability, default action, Review Severity, Detection Signals, ignore conditions, rationale, Source Groups, examples, and agent guidance.

Schema validation is structural only. It does not replace human review of CLReq-backed claims, locale handling, examples, or Review Suggestion quality.
